name: Updater Self Refresh Regression

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test updater self refresh
        run: bash tests/test-cog-update-self-refresh.sh
