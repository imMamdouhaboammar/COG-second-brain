name: Updater Path Safety Regression

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test updater path safety
        run: bash tests/test-cog-update-path-safety.sh
