name: Updater Remote Trust Regression

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test updater remote trust
        run: bash tests/test-cog-update-remote-trust.sh
