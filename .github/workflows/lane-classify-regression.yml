name: Lane Classifier Regression

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test lane classifier
        run: bash tests/test-lane-classify.sh
